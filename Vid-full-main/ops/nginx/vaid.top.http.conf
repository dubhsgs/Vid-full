server {
    listen 80;
    listen [::]:80;
    server_name vaid.top www.vaid.top;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://vaid.top$request_uri;
    }
}
